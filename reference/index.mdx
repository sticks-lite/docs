# Language Reference

This reference defines the Sticks Lite {{STICKS_LITE_VERSION_LABEL}} language.
Use it when you need exact syntax or behavior.

## Reference sections

| Page | Covers |
| --- | --- |
| Syntax | File shape, comments, indentation, statements, and keywords. |
| Values and Types | Numbers, text, booleans, `null`, lists, tuples, and dictionaries. |
| Variables and `DEFINE` | Assignment, constants, protected names, and scope. |
| Operators | Math, comparison, boolean, assignment, and indexing operators. |
| Conditionals | `if`, `orif`, `otherwise`, and boolean-only conditions. |
| Loops | `repeat`, `loopif`, `foreach`, `break`, and `continue`. |
| Functions | `new`, parameters, calls, return values, and arity errors. |
| Lists, Tuples, and Dictionaries | Literal forms, indexing, mutability, and operations. |
| Errors | Error names, formatting, and `attempt` handling. |
| Standard Library / Built-ins | Built-in functions and protected built-in names. |
| CLI Reference | Command behavior and file errors. |
| Public TypeScript API | Supported package exports for tools and embedding. |

## Version

The docs version is read from package metadata and rendered as
{{STICKS_LITE_VERSION_LABEL}}.

