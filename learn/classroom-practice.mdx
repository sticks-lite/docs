# Classroom Practice Programs

These programs are short enough for lessons, slides, clubs, and warmups.

## Greeter

```slite
name = ask "What is your name?"
say "Hello " + name
```

## Calculator

```slite
first = toNumber(ask "First number?")
second = toNumber(ask "Second number?")

say "Sum: " + toText(first + second)
say "Difference: " + toText(first - second)
say "Product: " + toText(first * second)
say "Quotient: " + toText(first / second)
```

## Grade checker

```slite
score = 87

if score >= 90:
    say "A"
orif score >= 80:
    say "B"
orif score >= 70:
    say "C"
otherwise:
    say "Keep practicing"
```

## List average

```slite
scores = [90, 85, 100]
total = 0

foreach score in scores:
    total += score

average = total / length(scores)
say "Average: " + toText(average)
```

## Dictionary profile

```slite
profile = {"name": "Maya", "age": 13}
profile["city"] = "Ridgewood"

say profile["name"] + " is " + toText(profile["age"])
say toText(profile)
```

## Safe number input

```slite
attempt:
    age = toNumber(ask "Age?")
    say "Next year: " + toText(age + 1)
when ValueError:
    say "Please enter a number."
when error:
    say "Something unexpected happened."
```

## Next step

Ask students to change one thing at a time: the prompt text, the thresholds, the
list items, or the function name.

