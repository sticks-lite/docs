# Classroom Programs

These programs are short enough for lessons, slides, or warmups.

## Greeter

```slite
name = ask "What is your name?"
say "Hello " + name
```

## Calculator

```slite
first = toNumber(ask "First number?")
second = toNumber(ask "Second number?")

say "Sum: " + toText(first + second)
say "Difference: " + toText(first - second)
say "Product: " + toText(first * second)
say "Quotient: " + toText(first / second)
```

## Grade Checker

```slite
score = 87

if score >= 90:
    say "A"
orif score >= 80:
    say "B"
orif score >= 70:
    say "C"
otherwise:
    say "Keep practicing"
```

## List Average

```slite
scores = [90, 85, 100]
total = 0

foreach score in scores:
    total += score

average = total / length(scores)
say "Average: " + toText(average)
```

## Dictionary Profile

```slite
profile = {"name": "Maya", "age": 13}
profile["city"] = "Ridgewood"

say profile["name"] + " is " + toText(profile["age"])
say toText(profile)
```

## Function Practice

```slite
say toText(square(6))

new square(x):
    return x * x
```

## Safe Number Input

```slite
attempt:
    age = toNumber(ask "Age?")
    say "Next year: " + toText(age + 1)
when ValueError:
    say "Please enter a number."
when error:
    say "Something unexpected happened."
```
